"""
app/agent.py — placeholder.

The real agent pipeline (SequentialAgent: extractor → policy → replier)
is implemented in FLOW-017 through FLOW-021.  This file is intentionally
minimal until then.

The old root_agent defined here (which accepted phone_number as a tool
parameter — a critical security finding) has been removed.  See
REVIEW_AND_PLAN.md §5 and §18 (Finding 1) for the full explanation.
"""
